import 'package:flutter/material.dart';

class SearchWebinarList extends StatelessWidget {
  const SearchWebinarList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('');
  }
}
